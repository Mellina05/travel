import React from 'react';
import { View, Text} from 'react-native';

/**
 * CreatePlanPage is the creation page of a plan.
 */

export function PlanPage({ navigation }) {

    return (
        <View>
            <Text>Plans:</Text>
        </View>
    );
}